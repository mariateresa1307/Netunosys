// Elements (tiny components that doesnt do anything by themselves ,may  have hardcoded data, usually needs to be coupled with other elements/components inside a component/page)

import Toast from "../elements/toast";
import Animate from "../elements/animatedRoute";
import SectionTitle from "../elements/sectionTitle";

export { Toast, Animate, SectionTitle };
